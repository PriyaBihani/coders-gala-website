const initialState = {
  issomething: null,
  alksdfj: true,
  use23asdklfjr: null,
  isA2343dmin: null,
};

export default function (state = initialState, action) {
  const { type } = action;

  switch (type) {
    default:
      return state;
  }
}
